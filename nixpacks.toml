[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build 2>&1 || (cat .next/error.log 2>/dev/null; exit 1)"]

[start]
cmd = "npm start"
