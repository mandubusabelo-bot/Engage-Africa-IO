[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["rm -rf .next node_modules/.cache && NEXT_IGNORE_INCORRECT_LOCKFILE=1 npm run build"]

[start]
cmd = "npm start"
