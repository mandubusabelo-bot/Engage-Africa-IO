[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["NODE_ENV=production NEXT_IGNORE_INCORRECT_LOCKFILE=1 npm run build"]

[start]
cmd = "npm start"
